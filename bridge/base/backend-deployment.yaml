#! backend-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: backend
    namespace: freelance
    labels:
        com.docker.compose.project: freelance
        com.docker.compose.service: backend
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: freelance
            com.docker.compose.service: backend
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: freelance
                com.docker.compose.service: backend
                com.docker.compose.network.default: "true"
        spec:
            containers:
                - name: backend
                  image: freelance-backend
                  imagePullPolicy: IfNotPresent
                  command: ['sh -c python manage.py migrate && python manage.py runserver 0.0.0.0:8000']
                  env:
                    - name: ALLOWED_HOSTS
                      value: "localhost,127.0.0.1"
                    - name: CORS_ALLOWED_ORIGINS
                      value: "http://localhost:5173"
                    - name: CSRF_TRUSTED_ORIGINS
                      value: "http://localhost:5173"
                    - name: DATABASE_URL
                      value: "postgresql://postgres:postgres@db:5432/uzinex"
                    - name: DEBUG
                      value: "True"
                    - name: REDIS_URL
                      value: "redis://redis:6379/0"
                    - name: SECRET_KEY
                      value: "change-me"
                  ports:
                    - name: backend-8000
                      containerPort: 8000
                  volumeMounts:
                    - name: app
                      mountPath: /app
            volumes:
                - name: app
                  hostPath:
                    path: C:\Users\Dell\Рабочий стол\Freelance\backend
